// Manga page hosting has been removed from the Succession-only archive.
// This empty compatibility export keeps chapter metadata consumers stable while
// the reader-specific surface is retired.
export const authorizedSuccessionChapterMedia = Object.freeze({});
